# --- Framework de la API ---
fastapi
uvicorn[standard]

# --- Base de Datos (PostgreSQL) ---
sqlalchemy       # ORM
psycopg2-binary  # Driver de PostgreSQL
alembic          # Para migraciones de base de datos

# --- Caché (Redis) ---
redis>=4.2.0

# --- Validación de Datos y Configuración ---
pydantic[email]
pydantic-settings

# --- Dependencias del Scraper ---
# (Se instalan aquí para que 'run_scraper.py' funcione)
selenium
beautifulsoup4
webdriver-manager
scikit-learn     # (Para el filtro inteligente de tu scraper)
lxml             # (Parser más rápido para BeautifulSoup)